package com.nuclearunicorn.serialkiller.game.ai.llm;

/**
 * Web build: {@code --llm-check} is a command-line diagnostic for the desktop build and
 * there is no local server here to diagnose. Kept only so the shared {@code Main} still
 * compiles; the browser entry point is {@code WebMain}, which never reaches this.
 */
public final class LlmDoctor {

    private LlmDoctor() {}

    public static int run() {
        return 0;
    }
}
